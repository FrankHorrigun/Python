# Напишите программу, которая на вход принимает 5 чисел и находит максимальное из них.
elements = []
for i in range(5):
    elements.append(int(input(f'введите {i+1} число: ')))
print(elements)
max = elements[0]
for i in range(1, len(elements)):
    if max < elements[i]:
        max = elements[i]
print(max)
